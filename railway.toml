[build]
buildCommand = "npm install --production=false"

[deploy]
startCommand = "node index.js"
```

Press **Ctrl+S** then:
```
git add .
git commit -m "fix: force npm install in toml"
git push origin main